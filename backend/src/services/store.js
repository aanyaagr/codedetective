const fs=require("fs"),path=require("path");
const DATA_DIR=path.resolve(__dirname,"../../data"),DATA_FILE=path.join(DATA_DIR,"store.json");
const initialData={users:[],sessions:[],seasons:[{id:"season-1",number:1,title:"Python Foundations",language:"python",unlocked:true}],cases:[{id:"case-001",seasonId:"season-1",number:"001",title:"THE DUPLICATE REPORTS",description:"The automated record system is duplicating critical case files.",difficulty:"MEDIUM",xpReward:250,prerequisiteCaseId:null,stages:["BRIEFING","LEARN","INVESTIGATE","CODE","EVIDENCE","SOLVE"],concept:"loops and conditions",challenge:{id:"challenge-001",language:"python",starterCode:"reports = []\n\nfor r in range(1, 6):\n    reports.append(r)\n    if r % 2 == 0:\n        reports.append(r)\n\nprint(\"Case Output:\", reports)",tests:[{id:"test-1",name:"No duplicate reports",stdin:"",expectedOutput:"Case Output: [1, 2, 3, 4, 5]"},{id:"test-hidden-1",name:"Hidden uniqueness invariant",stdin:"",expectedOutput:"Case Output: [1, 2, 3, 4, 5]",hidden:true}]},evidence:[{id:"ev-01",code:"EV-01",text:"The loop triggers a duplicate push on even iterations.",condition:"failed_test"},{id:"ev-02",code:"EV-02",text:"The output buffer violates the five-item uniqueness invariant.",condition:"failed_test"},{id:"ev-03",code:"EV-03",text:"A corrected loop produces exactly five unique reports.",condition:"passed_all_tests"}]}],progress:[],submissions:[],xpTransactions:[]};
function initializeStore(){fs.mkdirSync(DATA_DIR,{recursive:true});if(!fs.existsSync(DATA_FILE))write(initialData);}
function read(){initializeStore();return JSON.parse(fs.readFileSync(DATA_FILE,"utf8"));}
function write(data){fs.mkdirSync(DATA_DIR,{recursive:true});const tmp=`${DATA_FILE}.tmp`;fs.writeFileSync(tmp,JSON.stringify(data,null,2));fs.renameSync(tmp,DATA_FILE);}
function update(mutator){const data=read();const result=mutator(data)||data;write(data);return result;}
module.exports={initializeStore,read,write,update};
